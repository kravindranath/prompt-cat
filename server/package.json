{
  "name": "api-prompt-cat",
  "version": "1.0.0",
  "description": "map prompts to templates",
  "main": "index.js",
  "scripts": {
    "test": "npm run test",
    "dev": "nodemon index.js",
    "build": "esbuild index.js --bundle --platform=node --outfile=dist/index.js",
    "start": "npm run build && nodemon dist/index.js"
  },
  "author": "kravindranath",
  "license": "Apache-2.0",
  "dependencies": {
    "compromise": "^14.14.4",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "natural": "^8.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.1",
    "nodemon": "^3.1.9"
  }
}